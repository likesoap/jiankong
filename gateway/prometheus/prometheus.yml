global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: adguard
    metrics_path: /metrics
    static_configs:
      - targets:
          - 127.0.0.1:80
    metric_relabel_configs:
      - source_labels: [__name__]
        regex: adguard_.*
        action: keep
